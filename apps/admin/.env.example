# Server-only URL used by Server Components, actions, and the proxy.
INTERNAL_API_URL=http://localhost:8080

# Must match the API's INTERNAL_API_KEY. It bypasses rate limits only.
INTERNAL_API_KEY_SB=local-internal-api-key

# Keep false for local HTTP and true in the production Kamal config.
SECURE_COOKIES=false
